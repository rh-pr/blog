export type Post = {
    id: string
    title: string,
    url:string,
    description: string,
    content: string,
    createDate: string,
    comments: string
}

export type Menu = {
    text: string,
    path: string
}